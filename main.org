var int32 a = 5;

// Modify the variable
mod a = a + 1;

// Reallocate the variable to a larger type
realloc a int64;

// Use an if-else statement to check the value of a
if a > 0 do {
    return 1; // Return 1 if a is positive
} else if {a < 0} do {
    return -1; // Return -1 if a is negative
} else do {
    return 0; // Return 0 if a is zero
}

// Use a while loop to count from 0 to 9
var int32 i = 0;
while i < 10 do {
    // Increment i by 1
    mod i = i + 1;

    // Use continue to skip the rest of the loop body when i is 5
    if i == 5 do {
        continue; // Skip the rest of the loop body when i is 5
    }

    // Use break to exit the loop when i is 8
    if i == 8 do {
        break; // Exit the loop when i is 8
    }

    call add(i, 2); // Call the add function with i and 2
}

// Make a syscall to print hello world. Use raw linux syscalls
var [14]char hello = "Hello, World!\n";
syscall 1(1, hello, 14); // Syscall number 1 is for write in Linux

var int32 var1 = 3 * 3 + 4 - 5 ** 2 / 2 % 4

// Create a dummy add function
func add(a int32, b int32): int32 {
    return a + b; // Return the sum of a and b
};
